/* Estilos gerais */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

/* Header e navegação */
header {
    background-color: #333;
    color: #fff;
    padding: 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

header img {
    width: 150px;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
}

nav a {
    color: #fff;
    text-decoration: none;
}

/* Banner */
.banner {
    background-image: url('fundo.jpg');
    color: #fff;
    text-align: center;
    padding: 100px 0;
    background-size: cover;
}

/* Seções */
.sobre, .inscricao, .galeria, .video, #localizacao, #formulario {
    padding: 40px 20px;
    text-align: center;
}

.galeria .grid {
    display: flex;
    justify-content: space-around;
    gap: 20px;
}

footer {
    background-color: #333;
    color: #fff;
    text-align: center;
    padding: 20px;
}
